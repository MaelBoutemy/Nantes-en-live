{% extends 'base.html.twig' %}

{% block body %}

    {% for message in app.flashes('Compte créé avec succès!') %}
        <div class="flash-notice">
            {{ message }}
        </div>
    {% endfor %}

{% endblock %}
