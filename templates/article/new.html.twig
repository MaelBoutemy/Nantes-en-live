{% extends 'base.html.twig' %}

{% block title %}New article{% endblock %}

{% block page %}
    <form>
        <h1>Nouvel article</h1>

        {{ include('article/_form.html.twig') }}

        <a class="button" href="{{ path('article_index') }}">Les articles</a>

        {% if is_granted('ROLE_ADMIN') %}
        <a class="button" href="{{ path('article_index') }}">Accueil admin</a>
        {% endif %}

    </form>
{% endblock %}
