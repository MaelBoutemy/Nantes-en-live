{% extends 'base.html.twig' %}

{% block page %}

    <div>
    {{ include('user/_form.html.twig') }}

    <a class="button" href="{{ path('user_index') }}">back to list</a>
    </div>

{% endblock %}
